La app React (Vite) buildea bien y el backend en Render arranca sin errores, pero la aplicación queda pantalla completamente en blanco tanto en mobile como en desktop.

Necesito que hagas un debug exhaustivo de frontend runtime, NO de Prisma ni backend.

Tenés que revisar y verificar, en este orden:

Abrir la consola del navegador y buscar errores JavaScript en runtime (Uncaught TypeError, Module not found, Failed to fetch, etc).

Revisar todos los imports del frontend por posibles errores de mayúsculas/minúsculas (case-sensitive en Linux/Render).

Verificar que App.jsx realmente renderiza algo (hacer un render mínimo de texto para confirmar que React monta).

Revisar componentes críticos como:

HighlightViewer

Reels

cualquier componente nuevo agregado recientemente

Buscar useEffect o lógica que:

acceda a propiedades de objetos undefined

haga fetch sin try/catch

dependa de datos del backend que puedan venir vacíos

Verificar rutas (react-router) que puedan estar rompiendo el render inicial.

Verificar que el frontend esté apuntando al endpoint correcto del backend en producción, no a localhost.

Importante:

NO tocar Prisma

NO tocar migraciones

NO resetear base de datos

Objetivo final:
Encontrar exactamente qué archivo o línea hace que React crashee y corregirlo para que la app deje de quedar en blanco.