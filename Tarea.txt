Funciona perfecto. Ahora necesito refinar la navegación en `frontend/src/features/highlights/HighlightViewer.jsx`.

Quiero reemplazar los botones de flechas ("<" y ">") por gestos de deslizamiento (SWIPE) nativos para móviles, manteniendo el video fluido.

Por favor, realiza los siguientes cambios:

1. **LÓGICA DE SWIPE (Sin librerías extra):**
   - Agrega 3 estados locales (o refs) para manejar las coordenadas del toque: `touchStart`, `touchEnd`.
   - Define una distancia mínima de swipe (ej: 50px).
   - Implementa los handlers:
     - `onTouchStart`: Guarda la posición X inicial.
     - `onTouchMove`: Actualiza la posición X final.
     - `onTouchEnd`: Calcula la diferencia. Si es > 50px a la izquierda -> `onNext()`. Si es > 50px a la derecha -> `onPrev()`.

2. **INTEGRACIÓN EN EL JSX:**
   - Agrega los eventos `onTouchStart`, `onTouchMove` y `onTouchEnd` al contenedor principal del video (el mismo `div` que tiene `className="relative flex-1..."` y el `onClick={togglePlayPause}`).
   - **IMPORTANTE:** Asegúrate de que el `onClick` del video (pausa/play) NO se dispare si el usuario hizo un swipe. Puedes usar una pequeña bandera o lógica simple (si `distance > 10` no hacer toggle).

3. **LIMPIEZA DE UI:**
   - **Elimina** (o comenta) los botones `<button>` de "Anterior" y "Siguiente" que están flotando a la izquierda y derecha. Ya no los necesitamos en la vista móvil porque usaremos el swipe.

4. **PRECAUCIÓN:**
   - No toques la lógica de las Reacciones que acabamos de arreglar.
   - Mantén el `e.stopPropagation()` en los botones de cierre y reacciones para que no interfieran con el swipe.

Genera el código actualizado del componente.