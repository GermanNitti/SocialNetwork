El problema persiste. Aunque usamos `React.memo`, el componente `ReelPlayer` se sigue re-renderizando porque las funciones que recibe como props (`onNext`, `togglePlayPause`, etc.) cambian de referencia en cada render del padre.

Necesito aplicar un **"Hard Memo"** con una función de comparación personalizada.

Por favor, modifica la definición del `ReelPlayer` en `HighlightViewer.jsx` para que use un segundo argumento en `memo`.

Sigue esta estructura EXACTA:

1. **DEFINICIÓN DEL COMPARADOR:**
   ```javascript
   const ReelPlayer = memo(({ src, poster, isActive, onNext, onPrev, togglePlayPause, onTimeUpdate }) => {
       // ... todo tu código del player (useRef, useEffects, video tag) ...
   }, (prevProps, nextProps) => {
       // ESTA ES LA CLAVE: Devuelve true si NO queremos que se re-renderice.
       // Solo permitimos re-render si cambia la URL o el estado activo.
       // Ignoramos deliberadamente los cambios en las funciones (onNext, etc).
       
       return (
           prevProps.src === nextProps.src &&
           prevProps.isActive === nextProps.isActive
       );
   });



   REVISIÓN DE PROPS:

        Asegúrate de que ReelPlayer reciba src (o url) y isActive.

        Verifica que dentro del HighlightViewer (el padre), el ReelPlayer esté recibiendo correctamente estas props.

    SIN CAMBIOS EN LA LÓGICA:

        No muevas ni cambies la lógica de handleReaction ni el estado de reactionCounts. Solo blinda el componente del video.

Objetivo: Quiero que al dar like, el padre se actualice, pero el ReelPlayer sea COMPLETAMENTE IGNORADO por React gracias a la función de comparación personalizada.