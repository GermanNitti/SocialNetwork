El problema persiste y estoy seguro de que es porque definimos el componente `ReelPlayer` DENTRO de `HighlightViewer`. Esto causa que el video se desmonte y remonte en cada render del padre.

Por favor, reestructura `frontend/src/features/highlights/HighlightViewer.jsx` siguiendo estos pasos EXHAUSTIVOS:

1. **MOVER EL COMPONENTE (CRÍTICO):**
   - Mueve la definición de `const ReelPlayer = memo(...)` **FUERA** de la función `HighlightViewer`. Debe estar al nivel superior del archivo (top-level), antes o después del `export default`.

2. **DEFINICIÓN DE `ReelPlayer`:**
   - Asegúrate de que reciba las props: `({ src, poster, isActive, onNext, onTimeUpdate, togglePlayPause })`.
   - Mantén el `useRef` del video y el `useEffect` de control de reproducción DENTRO de `ReelPlayer`.
   - **IMPORTANTE:** Mantén el segundo argumento de `memo` (el comparador personalizado) para que solo se actualice si cambia `src` o `isActive`.

   ```javascript
   const ReelPlayer = memo(({ src, poster, isActive, onNext, onTimeUpdate, togglePlayPause }) => {
       const videoRef = useRef(null);
       
       // Lógica de play/pause basada en isActive...
       // useEffect para eventos...

       return (
           <div className="relative flex-1 overflow-hidden bg-slate-900" onClick={togglePlayPause}>
               <video 
                   ref={videoRef}
                   src={src}
                   className="absolute inset-0 w-full h-full object-contain"
                   // ... atributos ...
               />
               {/* Poster o placeholder si es necesario */}
           </div>
       );
   }, (prev, next) => {
       // Retorna TRUE si NO debe renderizarse de nuevo
       return prev.src === next.src && prev.isActive === next.isActive;
   });